<%= cp.page_title(
  content:  "Development",
  sub_text: "Short summary about current and future changes",
) %>


<%= cp.h2_tag(content: "Planned Features:") %>

<%= cp.ul_tag do %>
  <%= cp.li_tag { "Make github repository public" } %>
  <%= cp.li_tag { "Pull stories from QQ (questionablequesting.com)" } %>
  <%= cp.li_tag { "Ability to login using email authentication (no passwords)" } %>
  <%= cp.ul_tag do %>
    <%= cp.li_tag { "Authenticated users can have favorite story lists" } %>
    <%= cp.li_tag { "Authenticated users can be notified if a story is updated" } %>
  <% end %>
<% end %>

<%= cp.h2_tag { "Latest Changes:" } %>

<%= cp.ul_tag(add_class: "dl-horizontal") do %>
  <%= cp.li_tag { "Added NSFW and location filter" } %>
  <%= cp.li_tag { "Added AO3 (archiveofourown.org) story crawler" } %>
  <%= cp.li_tag { "Updated script to pull from SufficientVelocity worm subform" } %>
<% end %>

